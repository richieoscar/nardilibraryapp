import 'package:flutter/material.dart';
import 'package:nardilibraryapp/resources/app_style.dart';

class DepartmentBooks extends StatelessWidget {
  const DepartmentBooks({Key? key}) : super(key: key);
  static const route = 'departmentBooks';

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Under Development", style: AppStyle.headline1));
  }
}
